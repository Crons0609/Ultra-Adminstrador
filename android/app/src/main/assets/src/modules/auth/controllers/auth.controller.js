/**
 * @file auth.controller.js
 * @description Auth module logic controller.
 */
export class AuthController {
  static async handleLogin(email, password) {
    console.log('AuthController: Logging in', email);
  }
}